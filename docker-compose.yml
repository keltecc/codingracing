version: '2.3'

services:
  codingracing:
    build: .
    restart: always

  nginx:
    image: nginx
    restart: always
    ports:
      - 80:80
      - 443:443
    depends_on:
      - codingracing
    volumes:
      - ./certs:/certs/:ro
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - .:/var/www/:ro
      - ./logs:/var/log/nginx

